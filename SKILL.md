---
name: novel-to-drama
description: 将小说制作成 AI 短剧的总控技能。串联 shuohao-skills 改编与分镜、Codex imagegen 人物三视图、本地 ComfyUI＋MiniMax H3 视频生成、OpenMontage 剪辑、音画与口型同步、字幕合成、连贯性纠错及成片全检。用户要求小说到成片、继续短剧生产或全流程修片时使用；单独写作或单张出图使用对应专项技能。
---

# 小说到 AI 短剧

把原著变成可追溯、可续作的短剧生产项目。Codex 负责创作、调度和视听判断，确定性脚本负责参数绑定与文件检查。用户只要求创建/修改技能时，完成技能本身，不启动小说创作或图像、视频生成。

## 入口与范围

1. 先找用户提供的小说、已有项目与最近状态。已有剧本/人物图/视频时从缺失或失效的阶段继续。
2. 确认原文覆盖范围、计划集数/单集时长、画幅、画风、对白语言。能从上下文判断就记录后执行。未指定的创作偏好可用中文对白、9:16 成片、写实风作暂定方案；角色设定图用横向版式。小说缺失或全书改编范围不明时，继续环境准备，只询问真正缺少的内容，不假装读完全书。
3. “从小说做成短剧”覆盖前期、生成、剪辑、同步、字幕和检查；“只做剧本/预检”不授权后续生产。沿用用户已经给出的生成数量、预算及授权，不在每阶段重复确认。模型/供应商替换、超出约定成本或发布作品另行处理。
4. 创建项目在当前工作区，按宿主规则用 `work/` 放生产中间件、`outputs/` 放交付物。全局 skill 与 OpenMontage runtime 只存工具，不存新作品。

## 依赖与按需阅读

`{skill}` 是本文件目录，`{skills}` 优先取当前会话目录，其次 `$CODEX_HOME/skills`，未配置时取 `~/.codex/skills`。不要把 Windows 示例路径当跨机器常量。

| 阶段 | 首次进入时读取 |
|---|---|
| 改编、角色、美术、剧本、分镜 | `{skills}/shuohao-skills/SKILL.md`，以及当前阶段 `novel-outline` / `novel-characters` / `novel-art` / `novel-script` / `novel-storyboard` 的 `SKILL.md` |
| 角色三视图、场景图、分镜图 | 当前会话的 `imagegen` 技能；本技能 [前期与运镜](references/preproduction.md) |
| 本地视频生成 | `{skills}/openmontage/SKILL.md`；[ComfyUI 接线](references/comfyui-h3.md) |
| 剪辑、声音、字幕、全检 | [剪辑与全检](references/edit-and-review.md)；[项目数据与交付门](references/project-contract.md) |
| 安装路径、命令与来源 | [本机配置和来源](references/environment-and-sources.md) |

保留上游五份 JSON 的原生结构，运行各自 `validate`；不要把本技能的旁路状态表塞进上游 JSON 冒充支持的字段。本技能明确要求每个成片角色出图、实际视频、音画同步和全检，上游“可选/失败跳过”不能把这些变成已完成。

## 执行流程

### 0. 预检和建档

复制 [项目模板](assets/project.template.json)，填写真实范围与工具位置。运行 `scripts/preflight.py` 做只读检查，读取 OpenMontage 的 Agent Guide、Project Context、工具注册表和 `cinematic` 流水线，以及本次阶段导演说明；将 shuohao 产出映射到对应阶段，保留剧本对白，不能套成稀疏台词的预告片。

核对 ComfyUI 服务、H3 本地节点、模型栈、API 格式工作流、输出节点、图像/音频参考输入和 FFmpeg/ffprobe。预检在线不等于生成验证通过。模型未就绪不妨碍完成剧本与分镜。

### 1. 小说 → 改编 → 可拍剧本

用 `novel-outline` 建改编说明、分集大纲、角色/场景/道具清单，再用 `novel-characters`、`novel-art` 和 `novel-script` 互相校准。保留来源章节与改编变更，长篇记录已读/未读范围。给角色、场景、道具稳定 ID；剧本逐句有说话人、动作、情绪和时长预算。心理活动转为可见行动或明确画外音，不凭空改变核心动机与事件因果。

每个阶段运行其原生校验；不可用时记录缺失的校验和人工复核证据。剧本与资产事实不一致时修上游，不只改最终提示词。

### 2. 用 imagegen 生成每个人物的三视图

覆盖本次改编范围内全部可辨认的出镜角色，不能沿用上游默认“前 30 位”静默漏人。背景人群设计复用群演资产并登记，纯画外音标明无需外貌资产。

每人至少一张：同一人物的**正面、严格侧面、背面全身图**，同服装同尺度、头脚完整、统一光照与干净背景；可带半身面部基准。用 Codex 内置 `image_gen`，一个角色一次调用；生成后查看实际图像，检查面貌、比例、左右饰物和服装。服装/年龄变化建独立状态版本。

读取本地参考图后按当前工具签名传入参考；不把 CLI 的 `-i` 当工具参数。图像复制进项目并记录版本、提示词和文件哈希。失败或尚未检查的人物不能进入其镜头生成；其他已就绪镜头可继续。

### 3. 分镜、关键帧和运镜提示词

用 `novel-storyboard` 把剧本转成“生成段 → 段内切镜 → 每切关键帧”，保留节拍认领和逐字台词。读取 [前期与运镜](references/preproduction.md)，建立场景空间图、人物站位、视线、180°轴线、道具持握和进出镜方向。

每切写起始构图、主体动作、一个主运镜、速度/幅度、结束构图和接下镜条件。关键帧使用角色、场景、道具的已验收参考图。镜头表、H3 提示词和真实传入图片序号必须一致。实际工作流不支持多时间点参考时拆成单切生成，保留剪辑映射，不能只写 Picture 2 就声称第二张图已被使用。

### 4. 本地 ComfyUI＋MiniMax H3 生成

按 [接线说明](references/comfyui-h3.md) 选真实 H3 本地工作流，通过 OpenMontage `comfyui_video` 的 `minimax_h3_local` 路径执行。把提示词、seed、帧数、画幅和参考图真正绑定进 API JSON，先做离线结构检查。

在已授权的生产范围内先用一个有代表性的短段验证人物、对白、口型、运镜和音轨；样片合格后继续批次。单 GPU 默认串行排队，根据实测速度估时。逐段保存工作流哈希、模型/节点版本、参考资源、prompt_id、实测媒体信息与检查结论。

超时先查原任务或使用 `resume_prompt_id`，不重复提交。失败按报错定位；质量问题每镜默认最多 2 次有针对性的重做，计算/成本上限服从用户预算。仍失败的镜头列为阻塞，保留可用镜头继续独立工作，不无限循环。

### 5. 剪辑、音画同步与字幕合成

严格执行 [剪辑与全检](references/edit-and-review.md)。OpenMontage 负责素材清单、剪辑决策、合成和媒体分析；按真实工具 schema 操作，FFmpeg 处理必要的精确裁切、混音、字幕烧录和导出。

默认保留 H3 合格的原生对白与口型，音视频联动裁切。逐句定位实听台词并核对原剧本，检查嘴型、出声者、音效时点和剪辑后漂移。仅调整字幕不能修复错说/漏说台词；换配音必须复核口型，不把简单音轨替换称为口型同步。

输出校对过的 SRT 与 ASS，并交付干净母版和烧录字幕版；按实际对白做断句、对时、样式与遮挡检查，不依赖视频模型直接画字幕。

### 6. 局部纠错与跨镜连贯性检查

分三遍：单镜视听质量；相邻镜头/每集的空间、身份、动作与声音连续性；跨集剧情、道具状态、服装和音色连续性。问题记录到时间码和具体片段，选择重剪、改字幕、重混音或重生成的最小有效修复。

修改剧本、图像、视频、音轨或字幕后，使受影响的下游记录失效；重验相邻切点和重新导出的文件，不把旧报告贴到新成片上。

### 7. 最终全片检查与交付

重新读取用户原始需求，按最终导出文件独立做一遍完整检查。全量解码检查、逐句台词核对、音画/口型复核、字幕语义和烧录布局检查、跨镜/跨集检查都要有证据和覆盖范围。脚本通过、稀疏抽帧或 ASR 准确不能代替完整视听复核。

运行 `scripts/verify_delivery.py` 检查成片哈希、音视频轨、字幕时间、全时段检查记录与阻塞问题。它只验证结构与证据关联，不能自动证明人物一致或口型正确。当前工具不能完整看/听时标明 `partial` 和未检区间，不声称“全部检查通过”。

交付成片、母版、字幕、剧本/人物/分镜资料、生成清单、剪辑工程或 EDL、修复记录及终检报告。最终回复给出文件链接、实际完成范围、检查覆盖和剩余问题；不存在的问题不编造，不承诺零瑕疵。
